/* ==========================================================================
 * Commands module
 * Tauri command layer. Each submodule mirrors a backend domain and is wired
 * into the invoke_handler from lib.rs. Bodies are placeholders for now.
 * ========================================================================== */

pub mod drives;
pub mod history;
pub mod settings;
pub mod transfer;
